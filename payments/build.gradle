ext.configs = [
        androidLibrary: "${project.rootDir}/stripe-android/build-configuration/android-library.gradle",
]
apply from: configs.androidLibrary
apply plugin: 'kotlin-android'
apply plugin: 'signing'
apply plugin: 'maven-publish'

dependencies {
    api project(':stripe:payments-core')
    api project(':stripe:paymentsheet')

}

ext {
    artifactId = "stripe-android"
    artifactName = "stripe-android"
    artifactDescrption = "Stripe Android SDK"
}

